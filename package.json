{
  "name": "gemini-prompts",
  "version": "1.3.2",
  "description": "Gemini CLI extension for claude-prompts MCP server - hot-reloadable prompts with chains, gates, and symbolic syntax",
  "private": true,
  "type": "module",
  "dependencies": {
    "claude-prompts": "^2.1.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}
