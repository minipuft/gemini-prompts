{
  "name": "gemini-prompts",
  "version": "2.0.0",
  "description": "Gemini CLI extension for claude-prompts MCP server - hot-reloadable prompts with chains, gates, and symbolic syntax",
  "private": true,
  "type": "module",
  "dependencies": {
    "claude-prompts": "^1.4.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}
